Describe que hacen los siguientes comandos de git, escribe su descripcion al frente en una sola linea.

`git status`: Este comando se usa para obtener el estado actual del repositorio

git clone: Este comando se usa para clonar un repositorio en un nuevo directorio

git pull: Este comando se usa para obtener e integrar con otro repositorio o una sucursal local

git checkout: Este comando se usa para cambiar ramas o restaurar archivos de árbol de trabajo

git log: Este comando se usa para mostrar registros de confirmación

git branch: Este comando se usa para Listar, crear o eliminar ramas

git add: Este comando se usa para agregar los contenidos del archivo al índice

git commit: Este comando se usa para registrar cambios en el repositorio

git push: Este comando se usa para actualizar referencias remotas junto con objetos asociados

git merge: Este comando se usa para unir dos o más historias de desarrollo
`git clone`:

`git pull`:

`git checkout`:

`git log`:

`git branch`:

`git add`:

`git commit`:

`git push`:

`git merge`:
